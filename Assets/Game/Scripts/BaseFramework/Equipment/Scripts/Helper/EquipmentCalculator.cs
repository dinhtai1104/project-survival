public static class EquipmentCalculator
{
    public static float GetFinalStat(EquipmentEntity entity, int level, EStat type)
    {
        float rs = 0;
        // Base

        // Level

        // Bonus
        return rs;
    }
}
